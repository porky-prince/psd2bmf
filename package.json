{
  "name": "psd2bmf",
  "version": "1.0.0",
  "description": "Convert the PSD to BMF.",
  "main": "index.js",
  "scripts": {
    "start": "npm run build -- -w",
    "clean": "del-cli dist",
    "prebuild": "npm run clean",
    "debug": "node-nightly --inspect-brk dist/main.js",
    "test": "jest",
    "test1": "node dist/main.js",
    "build": "babel src -d dist --copy-files"
  },
  "author": "Kay",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.7.7",
    "@babel/core": "^7.7.7",
    "@babel/preset-env": "^7.7.7",
    "babel-jest": "^24.9.0",
    "del": "^5.1.0",
    "del-cli": "^3.0.0",
    "jest": "^24.9.0"
  },
  "dependencies": {
    "fs-extra": "^8.1.0",
    "psd": "^3.2.0"
  }
}
